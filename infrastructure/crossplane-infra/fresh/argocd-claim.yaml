apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: msdp-3tier-claim
  namespace: argocd
  labels:
    app.kubernetes.io/name: msdp-3tier-claim
    app.kubernetes.io/component: argocd-application
    app.kubernetes.io/part-of: msdp-platform
    test.msdp.com/type: fresh-no-touch
  annotations:
    argocd.argoproj.io/sync-wave: "2"
    platform.msdp.com/description: "Claim app - applies TierApp after foundation"
spec:
  project: default
  source:
    repoURL: https://github.com/msdp-platform/msdp-platform-core.git
    targetRevision: dev
    path: infrastructure/crossplane-infra/fresh/claim
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
      - RespectIgnoreDifferences=true
      - SkipDryRunOnMissingResource=true
      - ServerSideApply=true
    retry:
      limit: 10
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 5m
